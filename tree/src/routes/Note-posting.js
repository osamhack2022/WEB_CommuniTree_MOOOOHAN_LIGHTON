/* eslint-disable */
import { useState } from 'react';
import Button from 'react-bootstrap/Button';
import { useSelector } from "react-redux"
import { Table } from 'react-bootstrap'

function NoteP(){
    return(
        <div className='N-posting'>
        </div>
    )
}

export default NoteP